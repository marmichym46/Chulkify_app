import 'package:flutter/material.dart';

class registro extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: Text('Pagina Registro'),
      ),
      body: new Column(
        children: <Widget>[new Text('estamos en el formulario de registro')],
      ),
    );
  }
}
